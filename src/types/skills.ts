export type Skills = {
    id: number;
    name: string;
    level: number;
    img_url: string;
    desc: string;
    svg: string;
}

export type PersonalSkills = {
    id: number;
    name: string;
    svg: string
}

export type Languages = {
    id: number;
    name: string;
    level: string
}

export type Courses = {
    id: number;
    name: string;
    institution: string
}