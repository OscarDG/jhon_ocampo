---
import type { Images } from '../types/projects'

interface Props {
    id?: number,
    images: Images[]
}

const { images } : Props = Astro.props
---
<div class="relative grid grid-cols-3 gap-10 w-full max-w-280 max-h-vh h-auto p-5">
{
    images.map((image) => (
        <img src={image.image_url} loading="lazy" class="bento_image w-full h-fit max-h-130 rounded-2xl"/>
    ))
}
</div>